// MOVED -- superseded by src/app/config.ts's `env` export as part of the
// src/app/ restructure. It could not be deleted from this environment (a
// filesystem restriction on this specific mount blocks file deletion); it
// is unused and safe to `git rm` manually.
export {};
