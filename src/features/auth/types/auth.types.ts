// MOVED -- superseded by @lynkflow/types' auth/auth.types.ts (built from
// this file's earlier content, then renamed to a {Verb}Request/{Verb}Response
// convention and consolidated -- see that package's own commit for the
// full diff). Every consumer now imports directly from "@lynkflow/types".
// It could not be deleted from this environment (a filesystem restriction
// on this specific mount blocks file deletion); it is unused and safe to
// `git rm` manually, along with its now-empty parent `types/` directory.
export {};
