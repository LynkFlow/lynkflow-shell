// MOVED -- superseded by src/components/Checkbox as part of an earlier
// "generic reusable components go in src/components" restructure. It could
// not be deleted from this environment (a filesystem restriction on this
// specific mount blocks file deletion); it is unused and safe to `git rm`
// manually, along with its now-empty parent `common/` directory.
export {};
