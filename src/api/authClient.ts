// MOVED -- superseded by src/features/auth/api/ (authHttp.ts plus one file
// per endpoint group) as part of an earlier restructure, then further moved
// by the src/features/auth flatten. It could not be deleted from this
// environment (a filesystem restriction on this specific mount blocks file
// deletion); it is unused and safe to `git rm` manually, along with its
// sibling src/api/ directory (also unused/undeletable here).
export {};
